import TodoListItem from './TodoListIem'

const TodoList = () => {
    return (
        <ul className="todo-list">
            <TodoListItem />
            <TodoListItem />
        </ul>
    )
}

export default TodoList